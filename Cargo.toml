[package]
name = "alur"
version = "0.0.7"
authors = ["Spark <helllo@kamran.sh>"]
categories = ["command-line-utilities", "development-tools"]
edition = "2024"
homepage = "https://alur.happytoolin.com"
include = [
  "/Cargo.lock",
  "/Cargo.toml",
  "/LICENSE",
  "/README.md",
  "/THIRD_PARTY_NOTICES.md",
  "/aliases.json",
  "/src/**",
  "/tests/**",
]
keywords = ["package-manager", "task-runner", "node-shim", "npm", "cli"]
license = "GPL-3.0-only"
repository = "https://github.com/happytoolin/alur"
description = "Fast package-manager routing with short commands and an opt-in Node shim"

[profile.release]
opt-level = "z"
lto = "fat"
codegen-units = 1
panic = "abort"
strip = true
overflow-checks = false
debug = false
incremental = false

# The profile that 'dist' will build with
[profile.dist]
inherits = "release"
lto = "thin"

[dependencies]
anyhow = "1.0.102"
clap = { version = "4.6.0", default-features = false, features = ["std", "help", "usage", "error-context", "suggestions", "derive"] }
clap_complete = "4.6.0"
deno_task_shell = "0.29.0"
dirs = "6.0.0"
dunce = "1.0.5"
figment = { version = "0.10.19", features = ["toml", "env"] }
indexmap = { version = "2.12.0", features = ["serde"] }
is_executable = "1.0.5"
jsonc-parser = { version = "0.32.1", features = ["serde"] }
semver = "1.0.28"
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.149"
shlex = "1.3.0"
strum = { version = "0.27.2", features = ["derive"] }
thiserror = "2.0.18"
tokio = { version = "1.50.0", features = ["rt"] }
which = "8.0.2"
wildmatch = "2.6.1"

[dev-dependencies]
tempfile = "3.27.0"
