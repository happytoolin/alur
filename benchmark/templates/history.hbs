# Benchmark History

The repo intentionally keeps only the latest tracked benchmark report. Use `benchmark/LATEST.md` for the release-facing snapshot.

| Run | Report |
| --- | --- |
{{#each runs}}
| {{label}} | [{{file}}]({{fileRelative}}) |
{{/each}}
